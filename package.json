{
  "name": "fleetos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd services/order-service && npm install --ignore-scripts && npm run build",
    "vercel-build": "cd services/order-service && npm install --ignore-scripts && npm run build"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}