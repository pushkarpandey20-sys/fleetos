{
  "name": "fleetos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd services/order-service && npm install --ignore-scripts && npm run build",
    "start": "cd services/order-service && npm start",
    "dev": "cd services/order-service && npm run dev"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}